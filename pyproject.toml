[project]
name = "Dictore"
version = "1.6.1"
readme = "README.md"
description = "Offline voice-to-text dictation for Windows and Linux, using Whisper."
keywords = ["voice-to-text", "dictation", "whisper", "speech-to-text", "offline", "linux", "wayland", "transcription", "stt", "faster-whisper", "privacy"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: X11 Applications :: Qt",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: MIT License",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: POSIX :: Linux",
    "Topic :: Multimedia :: Sound/Audio :: Speech",
    "Topic :: Office/Business",
]

requires-python = ">=3.10"

dependencies = [
    "pyloid",
    "faster-whisper",
    "pynput",
    "sounddevice",
    "numpy",
    "pyperclip",
    "pyautogui",
    "keyring>=24",
    "soundfile>=0.12",
    "httpx>=0.27",
    "keyboard>=0.13.5; sys_platform == 'win32'",
    "evdev>=1.7.0; sys_platform == 'linux'",
    "nvidia-cublas-cu12; sys_platform == 'linux'",
]

[project.urls]
Homepage = "https://get-voice-flow.vercel.app/"
Repository = "https://github.com/infiniV/Dictore"
Issues = "https://github.com/infiniV/Dictore/issues"
Releases = "https://github.com/infiniV/Dictore/releases"

[dependency-groups]
dev = [
    "pyloid-builder",
    "pyloid-watcher",
    "ruff",
    "pytest",
]

[tool.pytest.ini_options]
testpaths = ["src-pyloid/tests"]
markers = [
    "network: tests that hit huggingface.co (slow, opt-in via DICTORE_NETWORK_TESTS=1)",
    "big_model: tests that download multi-GB models (opt-in via DICTORE_BIG_MODEL_TESTS=1)",
]
